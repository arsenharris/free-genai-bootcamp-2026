# Spanish Portal — Frontend

This folder contains a minimal Vite + React + TypeScript frontend scaffold using Tailwind CSS.

Quick start:

```bash
cd frontend-react
npm install
npm run dev
```

App routes created:
- /dashboard
- /study-activities
- /study-activities/:id
- /words
- /words/:id
- /groups
- /groups/:id
- /session
- /settings

The UI uses simple neobrutalism-inspired Tailwind styles in `src/styles.css`.
